# OpenCode Session Notes

## Сессия: 2026-04-15

### Что сделано

| Коммит | Описание |
|--------|---------|
| `1034d02` | Начальная структура проекта, SQLite + FTS5, веб-интерфейс |
| `f5bbc9b` | Поддержка хранилищ (HDD, DVD, SSD, NAS, network) |
| `7f3e546` | Идентификация носителей (catalog.json, volume_label), upsert логика |

### Реализованные фичи

- SQLite + FTS5 база данных
- Импорт книг из файловой системы
- Полнотекстовый поиск по названию, автору, описанию
- Управление хранилищами (UI + API)
- Автоопределение подключенных дисков
- Идентификация носителей через `catalog.json` или метку тома
- Upsert при повторном сканировании (обновление всех полей)
- Веб-интерфейс (каталог + хранилища)
- LAN доступ

### Структура проекта

```
C:\Library\
├── app/
│   ├── main.py           # FastAPI точка входа
│   ├── database.py       # SQLite + FTS5
│   ├── models.py         # Pydantic модели
│   └── routers/
│       ├── books.py      # API книг
│       ├── search.py     # API поиска
│       └── sources.py    # API хранилищ
├── services/
│   ├── importer.py       # Импорт + upsert логика
│   └── drives.py         # Обнаружение дисков, чтение catalog.json
├── web/
│   └── index.html        # SPA веб-интерфейс
├── library.db            # SQLite БД (создаётся автоматически)
└── config.py            # Конфигурация
```

---

## Возобновление разработки

### 1. Клонирование (новый ПК)

```powershell
git clone https://github.com/ksm-h-dev/HomeLibrary.git C:\Library
cd C:\Library
pip install -r requirements.txt
```

### 2. Запуск

```powershell
# Активировать виртуальное окружение (если есть)
.\venv\Scripts\activate

# Запуск сервера
uvicorn app.main:app --reload

# Или с LAN доступом
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 3. Тестирование

1. Открыть http://localhost:8000
2. Перейти на вкладку "Хранилища"
3. Нажать "Автоопределение" для поиска дисков
4. Добавить тестовое хранилище
5. Нажать "Сканировать"
6. Проверить книги на вкладке "Каталог"

---

## Следующие шаги (приоритет)

| # | Фича | Описание |
|---|------|---------|
| 1 | **CLI интерфейс** | Работа с каталогом через командную строку |
| 2 | **Тегирование книг** | UI для добавления тегов к книгам |
| 3 | **Экспорт каталога** | Экспорт в JSON/CSV |
| 4 | **Backup БД** | Автоматическое резервное копирование |

---

## Подготовка DVD диска для тестирования

```
E:\
├── catalog.json           ← {"id": "DVD-TEST-001", "name": "Тест DVD"}
├── programmy/
│   ├── book1.pdf
│   ├── book1.txt        ← метаданные
│   └── book1.jpg        ← обложка
└── psychologie/
    └── book2.djvu
```

---

## Git

```powershell
# Статус
git status

# Последние коммиты
git log --oneline -5

# Отправить изменения
git add -A
git commit -m "Описание изменений"
git push

# Получить изменения
git pull
```
